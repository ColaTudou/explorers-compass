# 探险家的罗盘 · 用户说明书

版本：V3.1（本地版 + PWA）
适用范围：两个人（情侣 / 好友 / 家人）一起记录生活、互相推荐去处、定期回顾

> 一句话：**像一本两个人合写的探险日记 + 一部很懂你们的推荐引擎。**
> 核心循环只有三步：**记录**（把每次出去玩的痕迹留下来）→ **推荐**（罗盘按你们的口味给建议）→ **回顾**（过去的记忆会在对的日子冒出来）。

---

## 1. 怎么打开

| 方式 | 怎么做 | 能用到啥 | 说明 |
|---|---|---|---|
| 手机 / 电脑「安装版」 | 见第 2 节 | 全部能力 | 体验最好，推荐 |
| 本地服务 | 双击项目里的 `启动本地服务.bat`，浏览器开 `http://localhost:8000` | 全部能力 | 本机自用最稳 |
| 直接双击 `index.html` | 双击就能开 | 基础记录功能 | **摄像头 / 通知 / 离线**用不了，日常记文字没问题 |

### 2. 装成 App（PWA）· 手机或电脑桌面一个图标点开

拿到链接后，**务必用手机自带的浏览器打开**（不要直接在微信里点开）：

- **iPhone（Safari）**：底部「分享」→「添加到主屏幕」
- **Android（Chrome）**：右上角菜单 →「安装应用」/「添加到主屏幕」
- **电脑（Chrome / Edge）**：地址栏右侧的「安装」图标

> ⚠ **微信 / QQ / 钉钉里点开链接是装不了的** —— 它们内置的浏览器不支持"添加到主屏幕"。
> 解决办法：点右上角「···」→ **「在浏览器打开」**，切到系统浏览器后再按上面的步骤装。
> （应用里也做了识别：「我的 → 安装到桌面 / 主屏幕」会检测到微信并直接告诉你怎么做。）

装好后从桌面图标进入，是全屏、没有地址栏的，跟原生 App 一样。
连不上网时也能打开已经看过的页面（离线缓存）。

> 📌 **正式链接**：`https://colatudou.github.io/explorers-compass/` —— 永久有效。
> （之前试用过的沙箱 / 云平台临时地址可能失效，一律以这个为准。）

---

## 3. 第一次使用：把你们两个人"绑定"起来

打开后默认带一份**示例数据**（一对叫"小鹿 & 阿柚"的演示用户），方便你先逛明白。
想绑成你们自己：

1. 底部 **「我的」** → 拉到最下 **「清空全部数据」** → 确认
2. 回到「我的」→ 点 **「现在绑定」**
3. 填 **你的昵称**、**TA 的昵称**，顺手挑个「魔法暗号」（颜色 + 形容词）
4. 点 **「生成绑定码」** → 看 6 位绑定码和暗号 → 点 **「模拟 TA 确认绑定」**
5. 出现「绑定成功」，顶部卡片就变成你们俩了

> 这台"本机模拟"版本里，两个人都用同一台设备：在「我的 → 当前视角」点一下，就能从 A 切到 B。
> 两台手机各自装的话，两边数据是独立的（见第 8 节怎么互通）。

---

## 4. 底部五个页签：日常逛哪

### 首页 · 今天该干嘛
- **幸福日历**：每天记录的小格子，评分越高颜色越暖
- **连续记录**：连着记的天数，连续 7 天有彩蛋
- **那年今日**：过去的今天你们去过哪（每年都会回来提醒你）
- **快捷区**：一键新建旅程、数据考古、AI 唤醒、相册导入

### 旅程 · 你们的探险档案
- 点右下角 **闪电按钮**（⚡）或按 **N**（电脑）→ **新建旅程**
- 新建后自动给你们随机分角色（可互换）：
  - **猎人**：负责拍照——全景、特写、偷拍三格
  - **诗人**：负责感受——气味、声音、温度 + 一段话
- 各自记完，等双方都提交 → **合并确认** → 归档
- 归档后：评分 ≤ 2 的地方/标签会被自动拉黑，再也不推荐
- 旅程详情还能：**备注**（自动保存）、**补批注**、标成**重要时光**

### 罗盘 · 今晚去哪
- 选「现在的心情 + 天气 + 预算」，点一下 → 罗盘转出 **3 个推荐** + 一段亲昵文案
- 推荐会避开：最近 14 天去过的、被拉黑的地方；节日/季节还会加权
- 下方还有 **随机任意门**（闭眼抽一个过去的地方）和**勇敢者之选**（试试没玩过的分类）

### 愿望 · 想一起做的清单
- 写下共同愿望（旅行、一顿饭、一件小事）
- 哪天实现了，把它和那次旅程关联起来，愿望清单会划掉并记上日期

### 我的 · 双人切换 + 更多功能 + 设置
- **当前视角**：A/B 切换（谁在操作）
- **更多功能**：星光集、待办、时光胶囊、年度报告（见第 5 节）
- **外部服务**：AI 大模型、真实天气、通知提醒、安装到桌面（都是可选的）
- **数据与外观**：导出 / 导入 / 深色模式 / 黑名单管理 / 闪电按钮开关 / 载入演示数据 / 清空

---

## 5. 更多好玩的功能

### 实时拍照
猎人界面点「拍照」→ 全屏取景器：白圈拍照可连拍，右上缩略图可删，左下角切前后摄，
点「完成(N)」一次带回，自动按顺序填进全景/特写/偷拍。相机不支持时自动改用系统相机。

### 数据考古复苏（第一次用没数据？从这里挖）
把过去半年的痕迹变成草稿旅程：
- **账单 CSV**：支付宝 / 微信导出的账单（只读商户名 + 日期，**不碰金额**）
- **粘贴文本**：从 App 复制一段文字进来
- **相册批量**：选一批照片，按拍摄日期自动分组
- **表格 / 网页**：点评 / 小红书 / 美团等导出的 CSV / HTML 表格（自动认"日期、名称"列）

解析结果先给你过目，可勾选、可改标题，确认后才写入草稿。

### AI 陪聊唤醒（草稿太多补不完？）
超过 3 天的草稿，罗盘会问 5 个小问题（是否/一两个词），答完拼成一段"回忆录"，
可改成你自己的话存档。没配 AI 模型也能用（本地模板）。

### 星光集
把**重要时光**做成三个视角：瀑布流（翻照片）、时间轴（按年看脉络）、分类（纪念日/旅行/第一次…）。

### 待办
两个人的小事清单：紧急度 1–5 颗星、预估耗时、截止日期、指派给谁。按紧急度排、已完成自动折叠。

### 时光胶囊
写一封信，选个解锁日期（一年后 / 半年后 / 三年后快捷键）。到那天首页会提醒，
点开有个开启仪式：先见 🔒，点「开启」才变 ✨ 展开。

### 年度探险报告
每年年底自动生成：探险次数、平均评分、分类分布、十二月热力、年度最佳、颜色云、标签云。
「打印 / 存 PDF」可以存成一页纸，建议横向无边距。

### 导出成故事书（可翻阅、可打印）· 我的 → 导出成故事书

把你们的记录排成**一本能翻的回忆录**，双击打开就能看，浏览器打印即存 PDF。

导出前选四项：
- **格式**：HTML（可翻阅 / 打印成 PDF）/ Markdown（贴到笔记、公众号）
- **范围**：全部 / 今年 / 仅已完成的（已归档）
- **分类**：全部，或只看「旅行」「美食」「探店」等某一类（出门回来想专门翻"我们去过哪"很好用）
- **封面**：经典封面 / 用最新的一张照片 / 用重要时光的照片 / 从相册选一张…（选一张你们最喜欢的照片当封面）
- **照片**：含照片 / 不要照片（纯文字，文件小很多）

书里有什么：
- **封面**：你俩的名字、时间范围、多少段记忆多少张照片（选照片封面时，上面是照片、下面是书名）
- **目录**：按年份跳转
- **正文**：按时间正序，每条 = 日期 + 地点 + 分类 + 评分 + 照片 + **两个人各自的文字和"闻起来/听起来/摸起来"** + 标签 + 备注 + 二次批注
- **想一起做的事**：愿望清单（含已完成打勾）
- **我们一起走过的路**：总段数 / 照片数 / 重要时光 / 愿望实现数 + 分类分布条

> 导出的是**自包含 HTML**（照片都打包在里面），发给谁都能直接看，不依赖网络。
> 打印建议：A4 纵向、页边距默认，CSS 已做"卡片不跨页切断"处理。

### 节日 / 季节彩蛋
元旦、情人节、妇女节、清明、五一、儿童节、暑假、万圣节、圣诞、跨年——罗盘会应景加权推荐。

### 深色模式
「我的 → 外观主题」：跟随系统 / 浅色 / 深色 循环切换。深色是"夜间暖纸"风，不刺眼。

### 电脑快捷键
`1–5` 切换页签 · `N` 新建旅程 · `F` 闪电存档 · `Esc` 关弹窗 · `?` 快捷键帮助

---

## 6. 天气、通知、AI（都是可选的）

- **真实天气**：罗盘页可开启 Open-Meteo 自动获取（或手动选城市）。没开就用"晴/雨"手动选，功能照常
- **通知提醒**：开启后页面开着时会提醒"那年今日 / 周五晚上一起吃饭吗 / 有草稿待唤醒"。
  注意：纯前端版本只能在你**开着页面**时提醒，真正的后台推送需要服务端，做不到
- **AI 大模型**：想用 AI 写更灵动的文案/出题，就在「我的 → 外部服务 → AI 大模型」填一个

- **语音输入（用说的）**：记录页点「🎙 用说的」即可。
  ① 填了 AI 大模型时：录音 → 调该服务商的语音转写接口（硅基流动填 `FunAudioLLM/SenseVoiceSmall`、
  阿里百炼兼容模式填 `paraformer-v2`、OpenAI/Groq 填 `whisper-1`）→ 自动变成文字；
  ② 没填或该服务商不支持：会教你用**手机输入法自带的麦克风**（键盘上的 🎙 键），最稳、中文最准；
  ③ 桌面 Chrome/Edge 在境外网络下走浏览器自带识别。
  ⚠ 网页自带识别在国内一般连不上（要访问 Google 服务器），属正常现象。
  OpenAI 兼容的端点（DeepSeek / 通义 / 智谱 / Moonshot / 本地 Ollama 都行）。
  **不填也完全能用**（本地模板），填了只是锦上添花

---

## 6.5 我的日常（只属于你自己的本子）

入口：**首页 → 我的日常**，或「我的 → 更多功能 → 📔 我的日常」。

- **私密**：默认只有你自己能看到；切到对方身份也不显示。
- **能记什么**：文字 + 照片 + 心情 + 天气 + 地点 + 分类（探店 / 游玩 / 趣事 / 心得 /
  运动 / 工作 / 居家 / 其他）+ 自定义标签。
- **每日一问**：写不出来时点「换一个」，配了 AI 会由 AI 出题。
- **时间线回看**：按日期分组，可搜关键词 / 标签 / 地点，还能看到「🕰 去年的今天」。
- **分享成旅程**：某一条想让对方看到，点「分享成旅程」——
  它会在「旅程」里生成一条新记录，**只填你这一侧，另一侧留给 TA 写**（双面叙事）。

## 6.6 两台手机真正配对（不再是"一个软件两个角色"）

入口：**我的 → 配对与同步（两台手机）**。

1. 点开后会**自动生成一条配对链接和一个二维码**（服务器地址已内置，不用填）。
2. 把链接或二维码发给对方（微信 / 短信都行）。
3. 对方点开链接（或扫码）→ **自动完成配对**，之后两边的数据就能互相同步。
4. 之后每次打开 App 会**自动同步一次**，也可以随时点「立即同步」。

几个要说明的：

- 服务器只是一个**加密信箱**：只存密文，看不到你们的任何内容。
- 链接里带着钥匙，**别发到公开的地方**；换密钥 = 换房间，需要重新发给对方。
- 配对完成后，建议双方都把 App **装到桌面 / 主屏幕**，体验更稳。
- 想看看对方同步了没：弹窗里的「看看 TA 同步了没」。

## 7. 你的数据在哪里 & 怎么备份

**数据存在这台设备浏览器的"专属小房间"里**（文字在小存储区，照片在大图库），
**不上传任何服务器**，第三方看不到。但小房间跟着浏览器走——清缓存 / 换浏览器 / 换设备 = 换个家。
所以备份必须"走出去"：

### 备份与同步（我的 → 备份与同步）

打开这个页面你会看到：

- **任务冒险（🎯）**：「我的 → 更多功能 → 任务冒险」，也心愿页右上角「开一局」。
  玩法：① 先去心愿写下想去/想吃/想做的事 → ② 开一局（日常 / 旅行），填一个**总预算**
  （只记数，不限制怎么花）→ ③ 系统派 4-7 个任务：双人任务要**两人都打卡**才算完成，
  个人任务分给谁就谁打 → ④ 打卡可写一句话、传照片、记花费 → ⑤ 结算后归档成经历，
  可单独生成故事录 → ⑥ 给这局打 1-5 星 + 标签，下次派得更合口味。
  配了 AI 大模型时任务由 AI 出题（失败自动用本地模板）；没配就用本地随机模板。

  **开一局时可以定三件事：**

  | 项目 | 说明 |
  |---|---|
  | 🎨 **主题** | 自己写（如「海边慢一天」），或点现成预设 / 「🎲 随机」。**任务会围着主题转**：没配 AI 时本地模板按主题关键词优先匹配（海边→踩水、拍海景；美食→探店、带特产）；配了 AI 就直接按主题出题 |
  | 📜 **规则** | 一行一条自己写，或「🎲 随机规则」（本地规则库，也会挑贴合主题的）、「✨ AI 定规则」（配了 AI 才有）。定了规则，AI 出的题就不许违反 |
  | 📍 **位置**（旅行模式） | 勾上「用当前位置」，开局取一次经纬度 + 地名（可失败，不影响开局）。拿到位置后，AI 会出**在当地就能做**的任务（老街、海边、山道这类），并带上当地天气做参考。定位只在开局那一刻用一次，不记录轨迹、不上传 |

  局中还能点「✨ AI 补 2 个」让 AI 按同一套主题/规则/位置再追加任务。

- **上次备份**：多久前备份过；到 7 天没备份，「我的」顶部会出现黄色的**去备份**提醒条
- **每 7 天提醒我备份**：可以关掉
- 三个手段，看你用哪台设备：

| 手段 | 适合 | 怎么做 |
|---|---|---|
| 📁 **备份到文件夹…** | 电脑（Chrome / Edge） | 弹出系统窗口，把存档写进**你指定的硬盘目录**（比如 `D:\罗盘备份`）。存完自动记下"上次备份" |
| 💡 **选网盘同步目录 = 自动上云** | 电脑 | 上面那个文件夹，直接选成 **坚果云 / OneDrive / 微云的本地同步目录**——存进去，网盘客户端就自动传了一份到云端，等于半自动云备份 |
| 📥 **下载备份** | 手机 / 任何设备 | 下载一个 JSON 文件（**自包含全部照片**），存到文件 App，再发到电脑 / 网盘 |

### 手机和电脑怎么互通

> **互通 = 同一个存档文件搬家**：A 设备「下载备份 / 备份到文件夹」→ 把文件发到 B 设备 → B 设备「导入数据」。
> 一台手机记的，导到电脑能接着看；电脑整理过的，导回手机随身带。纯前端版没有"账号自动云同步"，这个文件就是你们的"进度存档"，定期导出才不丢。

### 两人各用一台手机（真·双人模式）⭐

> 上面说的"互通"是**同一份数据搬家**（换设备用）。
> 而**两个人各自用手机记、内容合到一起**，靠的是「**合并导入**」——这是两件不同的事。

**一次性准备（每人各做一次）**：

1. 各自打开 `https://colatudou.github.io/explorers-compass/` → 添加到主屏幕
2. 各自「**我的 → 绑定**」→ **两边填完全相同的两个昵称**（比如都填"大熊 / 小鱼"）
   - ⚠️ 昵称必须一致 —— 合并时靠昵称认人，不一致会多出重复用户

**日常怎么配合**：

1. 一起出去玩 → **各自在自己手机上记自己那一侧**（你写你的感受，TA 写 TA 的）
2. 谁先记完都行 → 「**我的 → 导出全部数据**」→ 把 JSON 文件发给对方（微信发文件 / 网盘都行）
3. 对方 → 「**我的 → 导入数据**」→ **一定要选「合并」**
4. 合并后，同一条旅程的 A / B 两侧就自动拼到一起了 → 进旅程详情点「**合并确认**」

**「合并」和「覆盖」的区别（很重要）**：

| 选项 | 作用 | 什么时候用 |
|---|---|---|
| ✅ **合并（推荐）** | 把对方的内容并进来，**你自己的记录一条不丢**；同一条旅程的两侧自动拼接；愿望 / 待办 / 胶囊 / 黑名单也按 id 合并 | 两个人之间互通 |
| ⚠️ **覆盖** | 用这份文件**替换掉**本机全部数据 | 换新手机、恢复备份 |

**节奏建议**：一次出行结束就合并一次；或配合「备份与同步」的提醒，每周合并一次。

> 💡 **原理**：合并靠的是**每条记录的 id**。所以别在两台手机上重复建同一条旅程——
> 一方建好、导出给对方，对方导入后就会看到同一条，直接填自己那侧就行。

### 恢复

- 新设备 / 清缓存后：**我的 → 导入数据** → 选那个 JSON 文件 → **选「覆盖」**（一键还原，照片都在里面）
- 建议节奏：**每 7 天备份一次**（App 会提醒），重要日子（旅行结束、月底）随手导一份

---

## 8. 常见问题（FAQ）

**Q：链接打不开 / 显示"应用尚未发布"**
A：这是临时沙箱被回收了。找发链接的人换新地址；想长期用就第 1 节的本地服务方式。

**Q：拍照按钮没反应**
A：`index.html` 直接双击时浏览器不给摄像头权限。用 `启动本地服务.bat` 打开，或装成 App。

**Q：提示"存储空间不足"**
A：先「我的 → 导出全部数据」备份，然后删一些不用的旧照片，或「清空全部数据」重来。
（新版大图已自动进大图库，这个提示一般很少出现。）

**Q：为什么 TA 看不到我写的内容？**
A：这是**双面叙事**的设计——合并前双方只能看自己那一侧 + 对方有没有提交，看不到对方内容。
合并归档后才会合到一起。

**Q：推荐的地方怎么老避开某地？**
A：某次有人打了 ≤2 分，那个地点/标签会进**自动黑名单**。「我的 → 黑名单管理」能手动移出。

**Q：换了浏览器 / 清了缓存，数据没了？**
A：数据在本机浏览器里，换浏览器 = 换了一个家。记得定期导出备份（见第 7 节）。

**Q：可以三个人 / 一个家庭用吗？**
A：不可以。这版是双人版；多人模式需要账号与服务端，纯前端做不到。

**Q：这些"示例数据"能删掉吗？**
A：能。「我的 → 清空全部数据」；或「载入演示数据」随时换一批新的来玩。

---

## 9. 一句话边界（说在前面）

这个 App 是**纯前端、单机版**：
- 数据不出你的设备（隐私友好，但没有账号自动云同步）
- 通知只能页面开着时提醒
- 备份：手动导出 / 备份到文件夹；**选网盘本地同步目录即可半自动上云**
- 多人、真后台推送、账号级云同步，需要服务器才能做——不在这版范围内
