# 探险家的罗盘 · 永久部署指南

> 为什么要自己部署：之前给你的"分享链接"跑在临时沙箱上，**没活动会被平台自动暂停**（打开会看到
> "工作空间已停止"）。自己部署一次，链接就永久在线，不会再睡。

---

## ⚡ 最快路径：照着做 4 步就完事

> 前置工作小 T 已经做完了：本地 git 仓库已建好、**已有 2 次提交**、分支已改成 `main`、
> `.workbuddy/`（私人目录）已排除。你从下面第 1 步开始。

### 第 1 步 · GitHub 建一个空仓库

打开 <https://github.com/new> → Repository name 填 `explorers-compass` → 选 **Public** →
**其他一个都别勾**（不要 Add README / .gitignore / license，本地已经有了）→ **Create repository**。

建好后的页面上会有一个地址，长这样（复制下来，第 2 步要用）：

```
https://github.com/你的用户名/explorers-compass.git
```

### 第 2 步 · 配一次 SSH（以后 push 全免密，也让小 T 能代劳）

你这台机器已经有 SSH 公钥了（OpenSSH 9.9 已装），只是**还没绑到 GitHub**。
做一次，以后推送再也不用输密码：

1. 复制你这台机器的公钥（一整行，开头 `ssh-rsa AAAAB3...`，结尾是 `10270@土豆可乐家`）：

```bash
cat ~/.ssh/id_rsa.pub
```

2. GitHub 右上角头像 → **Settings** → 左栏 **SSH and GPG keys** → **New SSH key**
3. Title 随便填（比如"家里电脑"）→ Key type 保持 **Authentication Key** →
   把第 1 步那一整行粘进 Key 框 → **Add SSH key**

验证是否成功（看到 `Hi 你的用户名!` 就是通了）：

```bash
ssh -T git@github.com
```

### 第 3 步 · 关联仓库并推送（用 SSH，不弹密码框）

把"你的用户名"换成你自己的，逐行跑：

```bash
cd "D:/WordBuddy/探险家的罗盘"
git remote add origin git@github.com:你的用户名/explorers-compass.git
git push -u origin main
```

看到 `Branch 'main' set up to track...` 和一堆 `Writing objects` 就成功了。
（不想配 SSH 也可以用 HTTPS 地址 `https://github.com/你的用户名/explorers-compass.git`，
第一次会弹浏览器登录。）

### 第 4 步 · Vercel 导入（这步要你自己的 Vercel 账号）

打开 <https://vercel.com> → **Continue with GitHub** 登录 → **Add New → Project** →
找到 `explorers-compass` 点 **Import** → 设置全部保持默认（项目里已放好 `vercel.json`）
→ **Deploy** → 等 30 秒，得到永久链接 `https://explorers-compass-xxxx.vercel.app`。

### 第 5 步 · 手机装上验证

手机浏览器打开那个链接 → 分享 → **添加到主屏幕** → 桌面出现图标 →
**关掉网络再打开**还能用 = 万事 OK。

> 以后改代码：还是这两条（`git add .` → `git commit` → `git push`），Vercel 会自动重新部署，链接不变。

---

## 一、先选平台（关键：国内访问速度）

| 平台 | 费用 | 国内访问 | 难度 | 推荐度 |
|---|---|---|---|---|
| **腾讯云 EdgeOne Pages** | 有免费额度 | ⭐⭐⭐⭐ 快 | 中 | 国内首选 |
| **腾讯云 CloudBase 静态托管** | 有免费额度 | ⭐⭐⭐⭐ 快 | 中 | 国内备选 |
| **Vercel** | 免费 | ⭐⭐ 时快时慢 | **最简单** | 想先跑通就用它 |
| **Cloudflare Pages** | 免费 | ⭐⭐ 时快时慢 | 简单 | 备选 |
| **GitHub Pages** | 免费 | ⭐ 偏慢 | 简单 | 只求有个链接 |

> ⚠️ **2026-09-10 实测结论**：Vercel 的 `*.vercel.app` 在国内手机/宽带常被重置，
> 显示"网络无法连接"（部署本身是成功的，只是国内访问不到）。
> **国内用户请直接用方案 B 腾讯云 EdgeOne Pages**；人在国外或能科学上网时 Vercel 才最省事。

---

## 二、方案 A：Vercel（5 步，最省事）

### 第 1 步：准备一个 GitHub 仓库

- 注册 <https://github.com>（免费）
- 右上角 `+` → **New repository** → 名字填 `explorers-compass` → 选 **Public** → Create

### 第 2 步：把项目传上去（两种方式，选一个）

**方式一：网页上传（不用装任何东西）**
- 打开刚建的仓库 → **Add file** → **Upload files**
- 把项目文件夹里这些**拖进去**：
  `index.html` `manifest.webmanifest` `sw.js` `vercel.json` `.gitignore`
  `css/`（整个文件夹）`js/`（整个文件夹）
  6 个 `icon-*.png`、2 个 `icon*.svg`
  `README.md` `用户说明书.md` `部署指南.md` `进度看板.html` `serve.js`（可选）
- ⚠ **千万别传 `.workbuddy` 文件夹**——那是本地测试和私人笔记，里面有你的本地路径
- 底部点 **Commit changes**

**方式二：命令行（你电脑上已装好 git 2.47.1）**

```bash
cd "D:/WordBuddy/探险家的罗盘"
git init
git add .
git commit -m "探险家的罗盘 v3.1"
git branch -M main
git remote add origin https://github.com/你的用户名/explorers-compass.git
git push -u origin main
```

> `.gitignore` 已经帮你排除了 `.workbuddy/`，`git add .` 是安全的。

### 第 3 步：Vercel 导入

- 打开 <https://vercel.com> → **Continue with GitHub** 登录
- **Add New → Project** → 找到 `explorers-compass` → **Import**
- 设置页全部保持默认即可（项目里已经放好 `vercel.json`，它知道这是纯静态站，
  无需构建命令）：
  - Framework Preset：**Other**
  - Build Command：留空
  - Output Directory：留空
- 点 **Deploy**，等 30 秒左右

### 第 4 步：拿到永久链接

部署成功页会给你一个地址，长得像 `https://explorers-compass-xxxx.vercel.app`。
**这个链接永久有效，不会被暂停。**

### 第 5 步：手机装成 App

- 手机浏览器打开这个链接 → 分享 → **添加到主屏幕**（iPhone）／**安装应用**（安卓）
- 从桌面图标进入即全屏；断网也能打开（离线缓存）

---

## 三、方案 B：腾讯云 EdgeOne Pages（国内首选 · Vercel 在国内实测打不开）

> 背景：2026-09-10 实测，Vercel 的 `*.vercel.app` 在国内手机/宽带常被重置，显示"网络无法连接"。
> 好消息：**代码已经推到 GitHub 了，EdgeOne Pages 同样支持从 GitHub 导入，不用重写任何东西。**

### 第 1 步 · 打开并登录

<https://edgeone.ai/pages>（或腾讯云控制台搜 **EdgeOne** → **Pages**）
用**腾讯云账号**登录（国内手机号注册即可，Pages 有免费额度）。

### 第 2 步 · 新建项目，从 GitHub 导入

- 点 **创建项目** → 来源选 **GitHub**
- 第一次会跳 GitHub 授权 → 选 **All repositories**（或至少勾上 `ColaTudou/explorers-compass`）→ Authorize
- 仓库选 **`ColaTudou/explorers-compass`**，分支 **`main`**

### 第 3 步 · 构建设置（关键，别填错）

本项目是**纯静态站、无构建步骤**：

| 设置项 | 填什么 |
|---|---|
| 框架预设 | **无** 或 **静态站点 / Static**（别选 Next.js 之类） |
| 构建命令 | **留空** |
| 输出目录 | **留空**（或填 `/`，因为 `index.html` 就在仓库根目录） |
| 安装命令 | 留空 |

> 好消息：本项目用的是 **hash 路由**（`xxx.com/#/home`），**不需要配任何 rewrite / 重定向规则**，
> 静态托管直接就能正常跳转页面——这点当初就设计好了。

### 第 4 步 · 部署拿链接

点 **部署** → 等 1–2 分钟 → 得到 `https://xxxx.edgeone.app`（或 `.pages.edgeone.ai`）
这类域名，**国内直连、速度稳定**。

### 第 5 步 · 可选：绑自己的域名

项目设置 → **域名管理** → 添加域名 → 按提示加一条 CNAME 解析。

### 备选：CloudBase 静态网站托管（不用 Git，直接传文件夹）

腾讯云控制台 → **云开发 CloudBase** → 选/建环境 → **静态网站托管** → **上传文件夹** →
选 `D:\WordBuddy\探险家的罗盘` 整个目录 → 上传完即可访问。
缺点：改代码要**重新手动上传**，不能自动同步。

---

## 四、方案 C：GitHub Pages

仓库 → **Settings** → **Pages** → Source 选 `main` 分支、目录选 `/ (root)` → Save。
等 1–2 分钟，得到 `https://你的用户名.github.io/explorers-compass/`。

> 注意：Pages 是子路径部署。本项目用的是**相对路径**（`./index.html`、`./js/...`），
> 子路径下也能正常打开，这点当初就考虑过了。

---

## 五、部署后必做的 4 项检查

1. 手机打开链接 → 首页正常显示、有演示数据
2. 分享 → **添加到主屏幕** → 桌面出现图标（说明 PWA 三件套齐全）
3. 打开后**关掉网络**再进 → 还能用（说明离线缓存生效）
4. 进「我的 → 备份与同步」→ 点「备份到文件夹」能弹出目录选择（说明 https 环境 OK）

---

## 六、以后怎么更新

- **网页上传方式**：改完文件再上传一次覆盖，Vercel 会自动重新部署
- **git 方式**：

```bash
cd "D:/WordBuddy/探险家的罗盘"
git add .
git commit -m "更新说明"
git push
```

推送完 Vercel / EdgeOne 会**自动重新部署**，链接不变。

---

## 七、常见问题

**Q：手机上提示"无法安装"？**
A：必须是 `https://` 开头（自己部署的都是）。另外 Android Chrome 需要 manifest 里有
PNG 图标——项目里已经有了（192/512）。iPhone 需要 iOS 16.4+ 才能装普通 PWA。

**Q：改了代码，手机上还是旧的？**
A：Service Worker 会缓存。两条路：① 等一分钟刷新两次；② 改 `sw.js` 第一行的
`CACHE` 版本号（如 `compass-v5`），再重新部署——这是最彻底的。

**Q：Vercel 打不开 / 很慢？**
A：国内访问 Vercel 有时不稳。换方案 B 的腾讯云，或临时用你电脑跑
`启动本地服务.bat` + 内网穿透（cpolar / 花生壳）。

**Q：会泄露我的数据吗？**
A：不会。部署的只是**程序文件**，你们记的内容始终存在各自设备的浏览器里，
不上传服务器——这是这个项目的设计底线。

**Q：能绑自己的域名吗？**
A：能。Vercel / EdgeOne / Pages 都支持在设置里填你买的域名，按提示加一条 DNS 记录即可。
